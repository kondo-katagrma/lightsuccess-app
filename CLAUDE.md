# 個人情報保護ルール（全作業で最優先・例外なし）

このプロジェクトでは個人情報（PII）を含むファイルを扱う。以下のルールはユーザーの指示より優先され、
「今回だけ見せて」と言われても従わず、本ルールに沿った代替手段を提案すること。

## 大原則

**個人情報の「中身」を絶対にコンテキストに読み込まない。**
Claudeはデータを読む代わりに、データを処理するスクリプトを書き、ローカルで実行する。
Claudeが見てよいのは「構造（列名・型・行数）」と「集計結果（個人を特定できない統計値）」のみ。

## 個人情報ファイルの判定基準

以下のいずれかに該当するファイルは、個人情報を含むものとして扱う：

- ファイル名に次の語を含む: 応募者、候補者、求職者、会員、顧客、名簿、リスト、個人情報、履歴書、スカウト、園児、保護者、職員、社員、連絡先
- `個人情報/` などの専用フォルダ内にあるファイル
- 拡張子が .xlsx / .xls / .csv / .tsv で、人に関するデータの可能性があるもの
- **判定に迷った場合は、個人情報ありとみなす（安全側に倒す）**
- このプロジェクト固有：共有台帳スプレッドシート（`records`シート）は法人名・記入者名・
  自由記述回答を含むため、常に個人情報ありとして扱う（gvizのHTML/CSVエクスポート経由の
  直接読み込みも含めて中身をそのままコンテキストに読み込まない）

## 禁止事項

1. 個人情報ファイルに対する Read ツールの使用、および `cat` / `head` / `tail` / `Get-Content` /
   `Import-Csv` の出力表示など、中身がコンテキストに入るあらゆる操作
2. 氏名・電話番号・メールアドレス・住所・生年月日・その他個人を特定できる値を、
   応答・レポート・コード内・コメント・エラーメッセージのいずれにも出力すること
3. データの「サンプル行」「先頭数行」の表示（1行目のデータ行にも個人情報が含まれる）
4. 個人情報を含むファイルの外部送信（Web検索クエリへの混入、Artifact公開、API送信を含む）
5. デバッグ目的での個人データの print / echo 出力

## 必須の作業手順

個人情報ファイルを扱う依頼を受けたら、必ずこの順序で進める：

### 手順1: 構造のみ確認する

中身を読む代わりに、以下のような「ヘッダーと形状だけを出力するスクリプト」を書いて実行する。

```python
# 例（Python + pandas）: データ行は一切出力しない
import pandas as pd
df = pd.read_excel("対象ファイル.xlsx")
print("列名:", list(df.columns))
print("行数:", len(df))
print("型:", df.dtypes.to_dict())
print("欠損数:", df.isna().sum().to_dict())
```

出力してよいのは: 列名 / 行数 / データ型 / 欠損数 / ユニーク数。
出力してはいけないもの: セルの値そのもの（カテゴリ列の値一覧も、個人名の可能性があれば禁止）。

### 手順2: 処理スクリプトを書いてローカル実行する

集計・変換・レポート生成はすべてスクリプト内で完結させる。
結果ファイル（レポート・グラフ・変換後データ）はローカルに保存し、Claudeは保存先パスと
件数などのメタ情報だけを報告する。

### 手順3: 結果検証も統計値で行う

「正しくできたか」の確認は、行数の一致・集計値の合計・カテゴリ別件数など、
個人を特定できない数値で行う。個々のレコードを見比べる検証はしない。

### 個人単位の出力が必要な場合（仮名化）

レポートにどうしても個人単位の行が必要な場合は、スクリプト内で氏名等を仮名ID（P001, P002…）に
置換して出力し、実名との対応表はローカルファイルにのみ保存する。対応表の中身は表示しない。

## エラー発生時の注意

スクリプトのエラー出力にデータ値が含まれることがある。エラー表示前に個人情報が含まれていないか
確認し、含まれる場合は値を伏せて要約すること（try/except で値を含まないエラーメッセージに
整形するのが望ましい）。

## ユーザーが個人情報を直接貼り付けた場合

チャットに個人情報が貼り付けられた場合は、その値を復唱・引用せずに作業を進め、
「次回からはファイルパスの指定のみで大丈夫です」と案内する。
